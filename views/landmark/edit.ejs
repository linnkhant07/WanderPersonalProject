<%- layout("layouts/boilerplate")  %> 
<div class="row">
    <h1 class="text-center">Edit Landmark</h1>
    <div class="col-6 offset-3">
        <form action="/landmarks/<%= landmark._id %>?_method=PUT " method="POST" class="validatedForm" novalidate enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label"  for="title">Title: </label>
                <input class="form-control" id="title" type="text" name="landmark[title]" value="<%= landmark.title  %> " required>
                <div class="valid-feedback">
                    Looks good!
                </div>
                <div class="invalid-feedback">
                    Title cannot be invalid
                </div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="location">Location: </label>
                <input class="form-control" id="location" type="text" name="landmark[location]" value="<%= landmark.location %> " required>
                <div class="invalid-feedback">
                    Location cannot be invalid
                </div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="price">Estimated cost for one night stay: </label>
                <div class="input-group">
                    <span class="input-group-text">$</span>
                    <input type="text" id="price" class="form-control" name="landmark[price]" placeholder="0.00" value="<%= landmark.price %> " required>
                    <div class="invalid-feedback">
                        Price cannot be invalid
                    </div>
                </div>
            </div>
            <div class="mb-3">
                <div class="mb-3">
                    <label for="landmark[image]" class="form-label">Upload images: </label>
                    <input class="form-control" type="file" id="landmark[image]" name="landmark[image]"multiple>
                </div>
                <div class="invalid-feedback">
                    Image cannot be invalid
                </div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="description">Description: </label>
                <textarea class="form-control" name="landmark[description]" id="description" cols="30" rows="5" required><%= landmark.description %></textarea>
                <div class="invalid-feedback">
                    Description cannot be invalid
                </div>
            </div>
            <div class="mb-3">
            <h5> Delete Images </h5>
            <div class="row row-cols-1 row-cols-md-4 g-4 mb-3">
                <% landmark.images.forEach((image, i) => { %>
                    <div class="col">
                        <div class="card">
                            <img class="card-img" src="<%= image.thumbnail %>" alt="">
                            <div class="card-img-overlay text-end pt-1 pe-2">
                                <label class="visually-hidden" for="image-<%= i %>">Delete Image</label>
                                <input class="form-check-input" type="checkbox" name="deleteImages[]" value="<%= image.filename %>"  id="image-<%= i %>">
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
            </div>

            

            <div class="mb-3">
                <a class="btn btn-danger ms-0" href="/landmarks/<%= landmark._id %> ">Back to Landmarks</a>
                <button class="btn btn-success">Update Landmark</button>
            </div>
        </form>        
        <!-- <footer>
            <a class="btn btn-primary mb-3" href="/landmarks/<%= landmark._id %> ">Back to Landmarks</a>
        </footer> -->
</div>

